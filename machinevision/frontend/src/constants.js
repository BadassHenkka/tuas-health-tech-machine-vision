export const alarmDrawerWidth = 260;

// how fast the camera grows into view - in milliseconds
export const cameraGrowTimeout = 2500;

// interval for refetching/checking for new alarms - in milliseconds
export const alarmCheckInterval = 3000;
